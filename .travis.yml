services: docker

before_install:
- docker build -t moderncv-fontawesome .

script:
- python generate.py
- docker run -v $(pwd):/data moderncv-fontawesome brabban_cv.out.tex

deploy:
  provider: releases
  api_key: ${GH_API_KEY}
  file: brabban_cv.pdf
  skip_cleanup: true
  draft: true
